import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.bitquadrat.waterisright',
  appName: 'WaterIsRight',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
